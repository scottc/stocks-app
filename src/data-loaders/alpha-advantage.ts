// Another free source of stock data, but requires an API key.
// https://www.alphavantage.co/

// suppress eslint-plugin-unicorn(no-empty-file): Empty files are not allowed.
const foo = () => {};
foo();
